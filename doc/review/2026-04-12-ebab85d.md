# Code Review — 10개 이슈 일괄 해결 (d731496..ebab85d)

- **커밋 범위**: d731496..ebab85d (PR #12~#21, 10개 이슈)
- **날짜**: 2026-04-12
- **변경 파일**: 23개 (195 insertions, 36 deletions)
- **기술 심각도**: CRITICAL
- **품질 심각도**: HIGH

---

## 1. Architect Review (강현수) — HIGH

### 잘한 점
- GeneratorBeanConfig @Bean 반환 타입을 GeneratorUseCase 인터페이스로 교정
- RouteConfig lb:// 서비스 디스커버리 전환
- RateLimitFilter Caffeine Cache 교체

### 개선 필요
- [MEDIUM] TransactionEvent.kt:3 — fds-common/event가 fds-common/security.CardMasking import → 모듈 관심사 혼재
- [MEDIUM] TransactionEvent.fromRaw() — 도메인 이벤트가 마스킹 비즈니스 로직 직접 수행 → infrastructure adapter 책임
- [HIGH] CommonApiResponse — fds-common/web에 잔존 → 각 서비스 infrastructure로 분리 필요
- [LOW] {noop} 비밀번호 인코더
- [LOW] Gateway httpBasic → JWT 전환 필요

## 2. Security Review (박서진) — HIGH

### 취약점
- [CRITICAL] Generator SecurityConfig — {noop} 비밀번호 인코더. PCI-DSS Req.8 위반
- [HIGH] Gateway SecurityConfig — /api/** permitAll. OWASP A01 위반
- [HIGH] CSRF 비활성화 — JWT 검증 필터 부재 상태
- [MEDIUM] Kafka trusted.packages: com.gijun.fds.* 와일드카드
- [LOW] ${DB_PASSWORD:} 빈 기본값 — 미설정 시 빈 비밀번호로 접속 시도

### 권고사항
- {noop} → BCryptPasswordEncoder 즉시 교체
- /api/** 대신 역할별 세밀한 매핑 + JWT 필터 추가
- Kafka trusted.packages를 구체적 FQCN으로 한정
- ${DB_PASSWORD:} → ${DB_PASSWORD} (기본값 제거)

## 3. Performance Review (이도윤) — HIGH

### 성능 이슈
- [CRITICAL] RateLimitFilter.kt:42 — TokenBucket @Synchronized 경합. 10K TPS 동일 IP 시 스레드 대기열 폭증
- [CRITICAL] GeneratorService.kt:57~68 — start()에 Semaphore 없음. rate=10000 시 코루틴 무제한 spawn
- [HIGH] SecurityConfig httpBasic — 전 요청 인증 필터 체인 순회 오버헤드
- [HIGH] Caffeine expireAfterAccess만 사용 — expireAfterWrite 병행 권장

### 최적화 제안
- TokenBucket → AtomicLong + CAS 기반 락 제거
- start() 내부 Semaphore 동일 적용
- HikariCP/Kafka 기본값 점검 (maximumPoolSize=10, batch.size=16KB → 10K TPS 부족)

## 4. Code Quality Review (정하은) — MEDIUM

### 잘한 점
- CardMasking object + mask() 순수 함수, 단일 책임
- named argument + trailing comma 완벽 준수
- 도메인 예외 리네이밍 적절

### 개선 필요
- [MEDIUM] TransactionData.toString()에서 CardMasking.mask() 미사용 — 중복 로직
- [HIGH] SecurityConfig adminPassword 기본값 "admin" 하드코딩
- [LOW] "admin" 사용자명 하드코딩
- [LOW] GeneratorService 1000L 매직 넘버 → 상수 추출
- [LOW] fromRaw() 파라미터 12개 → 도메인 객체 수신 팩토리로 리팩토링

## 5. Testing Review (김태현) — HIGH

### 테스트 현황
| 파일 | 테스트 |
|------|--------|
| CardMasking.kt | ❌ 누락 |
| TransactionEvent.fromRaw() | ❌ 누락 |
| TransactionData.toString() | ❌ 누락 |
| RateLimitFilter.kt | ⚠️ 기존 테스트 (Caffeine 교체 후 검증 필요) |
| GeneratorService.kt | ⚠️ 부분 (lifecycle만, require/burst 미검증) |
| SecurityConfig (×2) | ❌ 누락 |
| RouteConfig.kt | ❌ 누락 |

### 누락된 테스트 시나리오
- CardMaskingTest: 정상 마스킹, 짧은 번호 경계값
- TransactionEventTest: fromRaw() 필드 매핑 검증
- TransactionDataTest: toString() 민감정보 미노출 검증
- GeneratorServiceTest: require() 위반 예외, burst 통계 누적

## 6. Domain Review (윤지아) — HIGH

### 비즈니스 로직 정확성
- [HIGH] PCI-DSS 마스킹 형식 부적합 — 카드 길이 노출, 표준은 앞6+뒤4
- [HIGH] TransactionData에 원본 cardNumber 잔존 — copy()/직렬화 유출 경로

### 도메인 모델링 피드백
- TransactionEvent 생성 시 마스킹 강제 구조는 올바름
- fromRaw()에서 원본 카드번호 파라미터 시그니처 잔존 → CardNumber VO 도입 권장
- 도메인 예외 리네이밍 (AccessDenied, AuthenticationRequired) 적절

---

## Tech Lead 종합 (최민준) — CRITICAL

### 기술 판정
보안(noop 인코더, Gateway permitAll)과 성능(@Synchronized 경합, 코루틴 무제한 spawn) 모두 프로덕션 배포 불가. CRITICAL.

### 이전 리뷰 대비 개선/퇴보
- ✅ 개선 4건: 카드번호 마스킹, ConcurrentHashMap→Caffeine, Gateway lb://, @Bean 반환타입
- ⚠️ 새 이슈: 마스킹 로직 위치 문제, noop 인코더, Gateway permitAll

### 반복 실수 감지
- **인증/보안 설계 후순위화** — 보안을 기능 완성 후 처리하는 패턴 반복

## Quality Lead 종합 (한소율) — HIGH

### 품질 판정
PCI-DSS 위반과 원본 카드번호 잔존은 운영 환경에서 유출 사고로 직결.

### 학습 포인트
"마스킹 객체를 만들었다고 보안이 완성되지 않는다." — CardMasking 도입 후에도 toString() 미적용, 원본 필드 잔존이라는 두 구멍이 남았다.

---

## Action Items (즉시 수정)
- [ ] {noop} → BCryptPasswordEncoder 교체 — 출처: 박서진
- [ ] Gateway /api/** permitAll 제거 + 인증 필터 추가 — 출처: 박서진
- [ ] TokenBucket @Synchronized → AtomicLong + CAS — 출처: 이도윤
- [ ] start() Semaphore 또는 코루틴 concurrency 제한 — 출처: 이도윤
- [ ] CardMasking PCI-DSS 표준 형식 수정 (앞6+뒤4) — 출처: 윤지아
- [ ] TransactionData 원본 cardNumber 필드 캡슐화 — 출처: 윤지아
- [ ] TransactionData.toString()에 CardMasking.mask() 적용 — 출처: 정하은
- [ ] SecurityConfig adminPassword 기본값 제거 — 출처: 정하은
- [ ] CardMasking/TransactionEvent 핵심 테스트 작성 — 출처: 김태현

## Tech Debt (기록만)
- CommonApiResponse fds-common/web 잔존 → 각 서비스 infrastructure로 분리
- TransactionEvent의 CardMasking import → 마스킹 책임 infrastructure adapter로 이동
- Caffeine expireAfterWrite 병행 적용
- Kafka trusted.packages 와일드카드 범위 축소
- ${DB_PASSWORD:} 기본값 제거
- fromRaw() 파라미터 12개 → 도메인 객체 수신 팩토리로 리팩토링
- GeneratorService 1000L 매직 넘버 상수화
- HikariCP/Kafka 기본값 튜닝
